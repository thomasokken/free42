PalmOS ARM version: progress dialog currently not functional. Fix it, or maybe
remove it (is it fast enough?). Also, the implementations of shell_read() etc.
could be speeded up by using caching to eliminate a lot of ARM-to-68K calls.

Plain text program import/export; HP-41 ROM image import.

Import/export of variables.

Directories.

"Sticky" complex numbers, which remember the mode (polar or rectangular) they
were entered in, and are always displayed in that mode.

Algebraic equations, a la HP-27S.

Fractions.
